1. Что будет выведено в результате выполнения фрагмента? Что изменится, если var  заменить на let? Что будет, если var заменить на const? 

var scope = "global";
function f( ) {
    alert(scope); 
    var scope = "local";
    alert(scope);
}
f(); 

2. Каков будет результат выполнения операции? Почему?

s = 1 + 2 + " blind mice";
t = "blind mice: " + 1 + 2;

Что произойдёт при вычислении? Объясните.

if ((a == null) && (b++ > 10)) stop( );


3. Написать программу, запрашивающую номер года и определяющую, високосный ли он. Для запроса используйте фунцкцию prompt(). Год считается високосным, если его номер делится на 4, но не делится на 400.

4. Написать программу, запрашивающую строку и изменяющую регистр букв. Например, строка 'The Quick Brown Fox' должна быть преобразована в 'tHE qUICK bROWN fOX'.

5. Написать программу, выводящую все подстроки строки
Пример : dog
Вывод : d, do, dog, o, og, g

6. Написать программу, конвертирующую объект в список пар [свойство, значение]


7. Написать функцию, переворачивающую целое число.
Пример: x = 32243
Результат : 34223

8. Написать программу, удаляющую из массива значения null, 0, "", false, undefined и NaN.

9. Написать функцию, которая в половине случаев вычисляет площадь круга заданного радиуса, а в половине случаев возбуждает исключение. Для получения псевдослучайных чисел воспользуйтесь функцией Math.random(), которая выдаёт числа в интервале 0..1.
Вызовите функцию несколько раз для проверки.
Вызовите функцию 100 раз и сосчитайте количество удачных и неудачных вызовов.

10. Напишите интерфейс, который принимает математическое выражение (в prompt) и вычисляет его значение с помощью eval.

При ошибке нужно выводить сообщение (ошибкой считается не только некорректное выражение, такое как 2+, но и выражение, возвращающее NaN, например 0/0). При корректном вычислении вывести результат.

После вычисления опять запросить выражение. Процесс продолжается до тех пор, пока не ввели пустую строку.


